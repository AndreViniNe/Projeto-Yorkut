describe('Testes da Rota de Home (/home)', () => {
  it('deve ter testes reais implementados no futuro', () => {
    // Este teste placeholder garante que o arquivo não falhe no CI.
    expect(true).toBe(true);
  });
});